name: package:cli_util
permissions: read-all

on:
  # Run on PRs and pushes to the default branch.
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/cli_util.yaml'
      - 'pkgs/cli_util/**'
  pull_request:
    branches: [ main ]
    paths:
      - '.github/workflows/cli_util.yaml'
      - 'pkgs/cli_util/**'
  schedule:
    - cron: "0 0 * * 0"

defaults:
  run:
    working-directory: pkgs/cli_util/

jobs:
  analyze:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        sdk: [dev]
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
    - uses: dart-lang/setup-dart@65eb853c7ba17dde3be364c3d2858773e7144260
      with:
        sdk: ${{ matrix.sdk }}
    - id: install
      name: Install dependencies
      run: dart pub get
    - name: Check formatting
      run: dart format --output=none --set-exit-if-changed .
      if: always() && steps.install.outcome == 'success'
    - name: Analyze code
      run: dart analyze --fatal-infos
      if: always() && steps.install.outcome == 'success'

  test:
    needs: analyze
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        sdk: ['3.7', dev]
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
    - uses: dart-lang/setup-dart@65eb853c7ba17dde3be364c3d2858773e7144260
      with:
        sdk: ${{ matrix.sdk }}
    - id: install
      name: Install dependencies
      run: dart pub get
    - name: Run tests
      run: dart test --test-randomize-ordering-seed=random
      if: always() && steps.install.outcome == 'success'
    - name: Run Windows Integration Test
      if: always() && matrix.os == 'windows-latest' && steps.install.outcome == 'success'
      run: ./test/win32_integration_test.ps1
      shell: pwsh
